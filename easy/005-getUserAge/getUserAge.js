// getUserAge

// Напиши функцію, яка приймає об’єкт користувача і повертає його вік.

function getUserAge(user) {
  return user.age;
}

// приклад
const user = { name: "Іра", age: 21 };
console.log(getUserAge(user)); // 21
